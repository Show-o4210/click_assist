官方 LevelClickTimes 完整导出（脚本无条数上限）。
文件名 = 关卡内部英文键（与 level_names.py / JSON 键一致）。
中文对照见仓库 README。

若要改某一关：优先用 GUI「编辑表」写 level_overrides.json；
或复制到 ../tables/同名.txt（优先级低于 JSON）。
格式：每行一个秒数（从关卡时间轴 0 起的绝对时间）。
